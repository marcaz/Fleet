#include <rtl.h>
#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include "main.h"
#include "leds.h"
#include "acc.h"
#include "uart.h"


char logmsg[MAXLOG];
char modem_packet[MODEMBUFF];


void put_log (const char *data){
	//----------------------------------------------------------------
	for(;*data != NULL; data++){
		//-------------------------------------------------------------------------
		USART_SendData(USART2, *data);			
		//------------------------------------------------------------------------- 
		while (USART_GetFlagStatus(USART2, USART_FLAG_TC) == RESET){}
		//------------------------------------------------------------------------- 
	}
	//----------------------------------------------------------------
}

void send_to_modem(const char *data){
	//----------------------------------------------------------------------------- 
	for(;*data != NULL; data++){
		//-------------------------------------------------------------------------
		USART_SendData(USART3, *data);		
		//------------------------------------------------------------------------- 
		while (USART_GetFlagStatus(USART3, USART_FLAG_TC) == RESET){}
		//------------------------------------------------------------------------- 
	}
	//----------------------------------------------------------------------------- 
	memset(modem_packet,NULL,sizeof(modem_packet));
	//----------------------------------------------------------------------------- 
}

